name: trigger-learnings

on: 
  # push:
    # branches:
    #   - main
  pull_request:
    branches:
      - main
    types:
      - opened
  workflow_dispatch:
  schedule:    
    - cron: "*/10 * * * *"    

jobs:
  test:
    name: learn
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello"
